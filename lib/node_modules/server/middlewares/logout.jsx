function logout(req, res) {
  if (req.cookies.token && req.cookies.token !== 'deleted') {
    res.cookie('token', 'deleted', {
      expires: new Date(0),
      httpOnly: true,
    });
  }

  res.redirect('/');
}

export default logout;
