.users {
  &__none {
    font-family--regular();
    margin-top: rem(30);
  }
  
  &__add {
    margin-top: rem(30);
  }
  
  &__add-icon::before {
    margin-right: 0.5em;
  }
  
  &__cta {
    margin: 0 rem(5);
  }
}