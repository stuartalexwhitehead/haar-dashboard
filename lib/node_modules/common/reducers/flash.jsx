function flash(state = null, action) {
  switch (action.type) {
    case 'ADD_FLASH':
      return {
        flashType: action.flashType || 'info',
        message: action.message || null,
      };
    case 'REMOVE_FLASH':
      return null;
    default:
      return state;
  }
}

export default flash;
