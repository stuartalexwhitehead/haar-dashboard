.field-input {
  &__label {
    font-family--demi();
    color: $primary--dark;
    display: block;
    margin-bottom: rem(10);
  }
  
  &__error {
    font-family--regular();
    color: $error;
    display: block;
    margin-bottom: rem(10);
    font-size: rem(12);
  }
  
  &__field {
    font-family--regular();
    font-size: rem(16);
    padding: rem(10);
    outline: none;
    border: 1px solid $grey--light;
    width: 100%;
    
    &:focus {
      border-color: $primary;
    }
  }
}