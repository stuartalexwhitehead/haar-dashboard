.pagination {
  margin-top: rem(30);
  text-align: center;
  
  &__results {
    font-family: $font-family;
    font-size: rem(14);
    color $primary--dark;
  }
  
  &__links {
    margin-top: rem(15);
  }
}